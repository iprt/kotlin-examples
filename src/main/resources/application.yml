server:
  port: 8090

spring:
  application:
    name: kotlin-examples
  mvc:
    throw-exception-if-no-handler-found: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.100.1.100:3306/zhuzhenjie?characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Root@123#
  redis:
    database: 15
    host: ENC(OlmpxaA9LXoI/KNt+lFfaYQY5dEQtLA0)
    port: 6379
    password: ENC(ukG21OjLJaT2HwdlXW2gLgT9BJ8j2RFo)
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
  mqtt:
    broker: tcp://172.100.1.100:1883
    username: username
    password: password
    clientId: ${random.uuid}
    defaultTopic: /topic/default
    topics:
      - /topic/default
      - /hello/world

mybatis-plus:
  mapper-locations: classpath*:mapper/*Mapper.xml

jasypt:
  encryptor:
    # 加密时的盐值
    password: ${spring.application.name}

logging:
  level:
    # mapper 日志输出
    org.example.hello.mapper: debug